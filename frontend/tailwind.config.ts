// Tailwind v4 — configuration is CSS-first via @theme in globals.css
// This file kept for tooling compatibility only
import type { Config } from "tailwindcss";

const config: Config = {};

export default config;
